packages:
  - packages/*

# pnpm 11 的写法（pnpm 10 叫 onlyBuiltDependencies，11 改成了 allowBuilds）。
#
# 这两个包的 postinstall 都得跑：electron 要下载对应平台的二进制，
# esbuild 要装平台包。不显式表态的话 pnpm 会以 ERR_PNPM_IGNORED_BUILDS 结束，
# 而且 exit 1 —— 连 pnpm start 之前的依赖检查都过不去。
allowBuilds:
  esbuild: true
  electron: true
