{
  "name": "kankanban",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "KankanBan - 以卡片为唯一原语的项目管理工具",
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r --workspace-concurrency=1 typecheck",
    "mcp": "node packages/mcp/src/bin.ts"
  },
  "engines": {
    "node": ">=22.18"
  }
}
